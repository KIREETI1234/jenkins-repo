pipeline {
    agent any
    
    stages {
        stage('Clone Repository') {
            steps {
                sh 'git clone https://github.com/KIREETI1234/jenkins-repo.git'  
            }
        }
        stage('Build') {
            steps {
                sh 'echo build successful'
        }
      }
    }
}
